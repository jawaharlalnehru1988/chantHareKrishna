import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PrabhupadaOnChantingPage } from './prabhupada-on-chanting.page';

describe('PrabhupadaOnChantingPage', () => {
  let component: PrabhupadaOnChantingPage;
  let fixture: ComponentFixture<PrabhupadaOnChantingPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PrabhupadaOnChantingPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
